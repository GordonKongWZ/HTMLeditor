/**
 * separator/index.js — Separator (divider) component entry point.
 *
 * This is the single import target for the separator block module.
 * It aggregates:
 *   - Model registration
 *
 * The separator is a non-editable atomic block; no paste or interaction
 * wiring is needed.
 */

import './separator.model.js';

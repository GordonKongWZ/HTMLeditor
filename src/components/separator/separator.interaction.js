/**
 * separator.interaction.js — Separator keyboard behavior.
 * Separators are non-editable; no keyboard interaction needed.
 */

export function attachSeparatorInteraction(_el) {
  // No-op: separator is a purely visual, non-editable block.
  // Extension point: add focus/blur effects if desired.
}
